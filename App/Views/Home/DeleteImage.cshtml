@{
    var image = UrlData[0];
    if (!image.IsEmpty() && image.IndexOf("/") < 1)
    {
        if (File.Exists(Server.MapPath("~/Content/Images" + image)))
        {
            File.Delete(Server.MapPath("~/Content/Images" + image));
        }
    }

    Response.Redirect("~/Profile");

}