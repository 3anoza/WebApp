@{
    ViewBag.Title = "About";
}

<h1 style="text-align:center">Загрузка фотографий</h1>
<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("About", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            <div>Выберете JPEG файл</div>
            <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

            <div class="example-1">
                <div class="form-group">
                    <label class="label">
                        <i class="material-icons">attach_file</i>
                        <span class="title">Добавить файл</span>
                        <input type="file" name="files">
                    </label>
                </div>
            </div>
            <input class="lol" type="submit" name="click" value="Подтвердить" />

            <p style="color:red;">@ViewBag.Error</p>
            //<div class="lol" style="color:red;">Недопустимый формат файла!<p>Загрузить можно только: .jpg, .jfif</p></div>
        }

        <style>
           .center {position: absolute;
  left: 50%;
  transform: translate(-50%);}
           .example-1 .form-group{padding:1em;margin:1em}
           .example-1 input[type=file]{outline:0;opacity:0;pointer-events:none;user-select:none}
           .example-1 .label{width:120px;border:2px dashed grey;border-radius:5px;display:block;padding:1.2em;transition:border 300ms ease;cursor:pointer;text-align:center}
           .example-1 .label i{display:block;font-size:42px;padding-bottom:16px}
           .example-1 .label i,.example-1 .label .title{color:grey;transition:200ms color}
           .example-1 .label:hover{border:2px solid #000}
           .example-1 .label:hover i,.example-1 .label:hover .title{color:#000}
        </style>
        <script>
            $(document).ready(function () {

                $('input[type="file"]').change(function () {
                    var value = $("input[type='file']").val();
                    $('.js-value').text(value);
                });

            });

        </script>
    </div>
</div>
<!--<form id="upload" enctype="multipart/form-data" method="post" action="#/?Upload" runat="server">
    <p>Path:</p>
    <code>
        <input id="uploadfile" type="file" />
    </code>
    <input type="submit" value="sumbit" id="sumbit" />

</form>
    -->
<!--xzz<button onclick="window.location.href='/Content/Images/meme.png'">Click!</button>-->
